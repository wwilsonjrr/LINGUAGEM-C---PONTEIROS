/*
1. Escreva um programa que declare um inteiro, um real e um char, e ponteiros para inteiro, real e char.
   Associe as variáveis aos ponteiros (use &).
   Modifique os valores de cada variável usando os ponteiros.
   Imprima os valores das variáveis antes e após a modificação.
*/

#include <stdio.h>
#include <stdlib.h>

// -----------------------------------------------------------------------------
// Função que imprime os valores das variáveis
// -----------------------------------------------------------------------------
void imprimeValores(int inteiro, float real, char caractere)
{
    printf("Inteiro: %d\n", inteiro);
    printf("Real: %.2f\n", real);
    printf("Caractere: %c\n", caractere);
}

// -----------------------------------------------------------------------------
// Função que modifica os valores das variáveis usando ponteiros
// -----------------------------------------------------------------------------
void modificaValores(int *ptr_int, float *ptr_float, char *ptr_char)
{
    *ptr_int = 20;
    *ptr_float = 10.75;
    *ptr_char = 'Z';
}

// -----------------------------------------------------------------------------
// Função principal
// -----------------------------------------------------------------------------
int main()
{
    // ----------- VARIÁVEIS REAIS (argumentos) -----------
    int numero = 10;          // variável inteira
    float valor = 5.5;        // variável real
    char letra = 'A';         // variável caractere

    // ----------- PONTEIROS (forma didática) -----------
    int *pNumero = &numero;     // ponteiro aponta para 'numero'
    float *pValor = &valor;     // ponteiro aponta para 'valor'
    char *pLetra = &letra;      // ponteiro aponta para 'letra'

    printf("Valores originais:\n");
    imprimeValores(numero, valor, letra);

    // Passa os ponteiros para a função
    modificaValores(pNumero, pValor, pLetra);

    printf("\nValores apos modificacao via ponteiros:\n");
    imprimeValores(numero, valor, letra);

    system("pause");
    return 0;
}
