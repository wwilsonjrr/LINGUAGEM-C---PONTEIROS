/*
1. Escreva um programa que declare um inteiro, um real e um char, e ponteiros para inteiro, real e char.
   Associe as variáveis aos ponteiros (use &).
   Modifique os valores de cada variável usando os ponteiros.
   Imprima os valores das variáveis antes e após a modificação.


     Frase correta para você usar (e até escrever em prova)

     &inteiro representa o endereço da variável inteiro, que é passado para a função para que ela possa modificar seu valor por meio de um ponteiro.

Nota:

| Código    | Significado      |
| --------- | ---------------- |
| int *p;   | Declara ponteiro |
| p = &x;   | Guarda endereço  |
| *p        | Acessa valor     |
| &x        | Endereço de x    |


int x = 10;
int *p = &x;


x → vale 10

p → vale 1000 (endereço)

*p → vale 10

*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída (printf)
#include <stdlib.h>

// -----------------------------------------------------------------------------
// Protótipo das funções
// -----------------------------------------------------------------------------
void imprimeValores(int inteiro, float real, char caractere);
void modificaValores(int *ptr_inteiro, float *ptr_real, char *ptr_char);


// -----------------------------------------------------------------------------
// Função principal
// -----------------------------------------------------------------------------
int main()
{
    int INTEIRO = 10;          // Declara e inicializa um inteiro
    float REAL = 5.5;          // Declara e inicializa um real (float)
    char CARACTERE = 'A';      // Declara e inicializa um caractere

    printf("Valores originais:\n"); // Cabeçalho
    imprimeValores(INTEIRO, REAL, CARACTERE); // Chamada da função de impressão

    // Modifica os valores usando ponteiros
    modificaValores(&INTEIRO, &REAL, &CARACTERE); // Passa os endereços para a função modificar as variáveis

    printf("\nValores apos modificacao via ponteiros:\n"); // Cabeçalho
    imprimeValores(INTEIRO, REAL, CARACTERE); // Exibe os valores atualizados

    system("pause");
    return 0; // Indica término correto do programa
}

// -----------------------------------------------------------------------------
// Função que imprime os valores das variáveis
// -----------------------------------------------------------------------------
void imprimeValores(int inteiro, float real, char caractere)
{
    printf("Inteiro: %d\n", inteiro);        // Exibe o valor do inteiro
    printf("Real: %.2f\n", real);            // Exibe o valor do real com duas casas decimais
    printf("Caractere: %c\n", caractere);    // Exibe o valor do caractere
}

// -----------------------------------------------------------------------------
// Função que modifica os valores das variáveis usando ponteiros
// -----------------------------------------------------------------------------
void modificaValores(int *ptr_inteiro, float *ptr_real, char *ptr_char)
{
    *ptr_inteiro = 20;    // Modifica o valor do inteiro via ponteiro
    *ptr_real = 10.75;    // Modifica o valor do real via ponteiro
    *ptr_char = 'Z';      // Modifica o valor do caractere via ponteiro
}
